# 🚀 免费部署到 GitHub Pages

你的代码已经准备就绪！只需 3 步，5 分钟内完成部署。

---

## ✅ 已完成

- ✅ Git 仓库已初始化
- ✅ 所有文件已提交
- ✅ 代码已准备好推送

---

## 📝 部署步骤

### 第 1 步：创建 GitHub 仓库（2 分钟）

1. **访问 GitHub**
   - 打开 https://github.com
   - 登录你的账号（没有就先注册）

2. **创建新仓库**
   - 点击右上角 "+" → "New repository"
   - 仓库名称：`x402-mcp-server`（或你喜欢的名字）
   - 描述：`AI Agent Data Service - 首个专为 AI Agents 设计的链上数据服务`
   - 选择：**Public**（公开，才能使用免费的 GitHub Pages）
   - ⚠️ **不要勾选** "Initialize this repository with a README"
   - 点击 "Create repository"

3. **复制仓库地址**
   - 你会看到一个页面，显示类似这样的地址：
   ```
   https://github.com/你的用户名/x402-mcp-server.git
   ```
   - 复制这个地址

---

### 第 2 步：推送代码到 GitHub（1 分钟）

在终端运行以下命令（替换成你的仓库地址）：

```bash
# 进入项目目录
cd ~/x402-mcp-server

# 添加远程仓库（替换成你的仓库地址）
git remote add origin https://github.com/你的用户名/x402-mcp-server.git

# 推送代码
git push -u origin main
```

**示例**（假设你的用户名是 `crypto-dev`）：
```bash
git remote add origin https://github.com/crypto-dev/x402-mcp-server.git
git push -u origin main
```

**如果提示输入用户名和密码**：
- 用户名：你的 GitHub 用户名
- 密码：使用 Personal Access Token（不是账号密码）
  - 如何获取 Token：https://github.com/settings/tokens
  - 点击 "Generate new token (classic)"
  - 勾选 "repo" 权限
  - 复制生成的 token 作为密码

---

### 第 3 步：启用 GitHub Pages（2 分钟）

1. **进入仓库设置**
   - 在 GitHub 上打开你的仓库
   - 点击 "Settings"（设置）

2. **找到 Pages 选项**
   - 在左侧菜单找到 "Pages"
   - 点击进入

3. **配置 Pages**
   - **Source**（源）：选择 `Deploy from a branch`
   - **Branch**（分支）：
     - 选择 `main`
     - 目录选择 `/website`（重要！）
   - 点击 "Save"

4. **等待部署**
   - 通常 30 秒到 2 分钟
   - 页面会显示：
   ```
   Your site is live at https://你的用户名.github.io/x402-mcp-server/
   ```

---

## 🎉 完成！

### 你的网站地址

```
https://你的用户名.github.io/x402-mcp-server/
```

**示例**：
```
https://crypto-dev.github.io/x402-mcp-server/
```

---

## 🔍 验证部署

### 检查网站是否上线

1. 打开浏览器
2. 访问你的网站地址
3. 应该能看到你的产品网站

### 如果看不到

1. **等待几分钟** - 首次部署可能需要 2-5 分钟
2. **检查 Actions** - 在仓库中点击 "Actions" 查看部署状态
3. **清除缓存** - 按 Ctrl+Shift+R（Windows）或 Cmd+Shift+R（Mac）
4. **检查配置** - 确保在 Pages 设置中选择了 `/website` 目录

---

## 📱 分享你的网站

### Twitter 文案模板

```
🚀 推出 x402 AI Agent 数据服务！

✅ 实时链上数据
✅ 跨链价格聚合
✅ AI 原生微支付
✅ 按需付费（$0.0003起）

免费试用：https://你的用户名.github.io/x402-mcp-server/

#AI #DeFi #Web3 #x402
```

### 社区分享

- **x402 Discord**：https://discord.com/invite/cdp
- **Product Hunt**：https://www.producthunt.com/
- **Reddit**：r/defi, r/CryptoTechnology

---

## 🔧 后续更新

### 如何更新网站

每次修改后：

```bash
cd ~/x402-mcp-server

# 修改文件...

# 提交更改
git add .
git commit -m "Update website content"
git push

# 等待 1-2 分钟，网站自动更新
```

### 自定义域名（可选）

1. 购买域名（推荐 Namecheap、GoDaddy）
2. 在 GitHub Pages 设置中添加 "Custom domain"
3. 在域名商配置 CNAME 记录
4. 详细教程：https://docs.github.com/en/pages/configuring-a-custom-domain-for-your-github-pages-site

---

## 🆘 常见问题

### Q: 推送时提示 "Permission denied"？

**A**: 需要配置 SSH 密钥或使用 Personal Access Token

**使用 Token（推荐）**：
1. 访问 https://github.com/settings/tokens
2. 点击 "Generate new token (classic)"
3. 勾选 "repo"
4. 复制 token
5. 推送时用 token 作为密码

**使用 SSH（高级）**：
```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
cat ~/.ssh/id_ed25519.pub
# 复制输出内容，添加到 GitHub → Settings → SSH keys
```

### Q: 网站显示 404？

**A**: 检查以下：
1. Pages 设置中是否选择了 `/website` 目录
2. 等待 2-5 分钟让部署完成
3. 确认 Actions 中的 Workflow 成功

### Q: 样式不显示？

**A**:
1. 检查浏览器控制台错误
2. 确认 `style.css` 在 `website/` 目录
3. 清除浏览器缓存

### Q: 想要 HTTPS？

**A**: GitHub Pages 自动提供 HTTPS，网址是 `https://...`

---

## 📊 免费额度

GitHub Pages 完全免费，包括：

- ✅ 无限流量
- ✅ 无限访问次数
- ✅ HTTPS 证书
- ✅ CDN 加速
- ✅ 自动部署

唯一限制：
- 仓库大小 < 1GB
- 网站大小 < 1GB
- 每月带宽 < 100GB

（对于你的网站来说，这些限制完全够用）

---

## 🎯 下一步

1. ✅ **验证网站**：访问你的网站地址
2. ✅ **更新链接**：把网站地址加到 README.md
3. ✅ **开始推广**：在 Twitter、Discord 分享
4. ✅ **收集反馈**：询问用户意见

---

## 🎉 祝贺！

你的产品网站已经免费部署到互联网！

**网站地址**：`https://你的用户名.github.io/x402-mcp-server/`

全世界都能访问！🌍
